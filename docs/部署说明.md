# JiETNG 文档部署说明

:::tip ✅ 多语言支持已启用
文档现已支持三种语言：English / 日本語 / 简体中文
- 核心页面已完整翻译（首页、绑定指南、FAQ）
- 其他页面有占位符（无404错误）
:::

## 🚀 快速开始

### 最简单方式：使用快速启动脚本

```bash
cd /Users/matsuki/Desktop/JiETNG/docs

# 开发模式（推荐先试这个）
./quick-start.sh dev

# 或者构建生产版本
./quick-start.sh build

# 预览生产版本
./quick-start.sh preview
```

---

## 📋 两种部署方式详解

### ✅ 方式 A: GitHub Pages（推荐 - 自动化）

**优点：**
- ✅ 完全自动化
- ✅ 免费托管
- ✅ 每次push自动更新
- ✅ HTTPS支持
- ✅ 全球CDN加速

**步骤：**

#### 1. 推送代码到GitHub

```bash
cd /Users/matsuki/Desktop/JiETNG

git add .
git commit -m "添加文档网站和自动部署"
git push origin main
```

#### 2. 启用GitHub Pages

访问：https://github.com/Matsuk1/JiETNG/settings/pages

设置：
- **Source**: Deploy from a branch
- **Branch**: `gh-pages`
- **Folder**: `/ (root)`
- 点击 **Save**

#### 3. 等待部署完成（约2分钟）

查看进度：https://github.com/Matsuk1/JiETNG/actions

#### 4. 访问网站

```
https://matsuk1.github.io/JiETNG/
```

#### 以后更新文档：

```bash
# 修改docs/下的文件后
git add docs/
git commit -m "更新文档"
git push origin main
# GitHub会自动重新部署！
```

---

### ✅ 方式 D: 本地构建

**优点：**
- ✅ 完全控制
- ✅ 可部署到任何服务器
- ✅ 本地预览快速
- ✅ 无需GitHub账号

**三种使用方式：**

#### D1. 开发预览（推荐）

```bash
cd /Users/matsuki/Desktop/JiETNG/docs

# 使用脚本
./quick-start.sh dev

# 或手动执行
npm install
npm run docs:dev
```


**特点：**
- 修改文件自动刷新
- 适合编写和预览
- 不生成静态文件

#### D2. 本地构建 + 预览

```bash
cd /Users/matsuki/Desktop/JiETNG/docs

# 使用脚本
./quick-start.sh build
./quick-start.sh preview

# 或手动执行
npm install
npm run docs:build
npm run docs:preview
```

**输出：** `.vitepress/dist/` 目录

#### D3. 构建后部署到服务器

```bash
# 1. 本地构建
cd /Users/matsuki/Desktop/JiETNG/docs
npm run docs:build

# 2. 上传到服务器
scp -r .vitepress/dist/* user@server:/var/www/jietng-docs/

# 或使用rsync
rsync -avz .vitepress/dist/ user@server:/var/www/jietng-docs/
```

---

## 🛠️ 常见问题修复

### 问题1: npm权限错误

```bash
# 方案1（推荐）
sudo chown -R 501:20 "/Users/matsuki/.npm"

# 方案2
rm -rf ~/.npm
mkdir ~/.npm

# 方案3
npm cache clean --force
```

### 问题2: 依赖安装失败

```bash
cd /Users/matsuki/Desktop/JiETNG/docs

# 清除并重新安装
rm -rf node_modules package-lock.json
npm install
```

### 问题3: GitHub Pages显示404

检查 `docs/.vitepress/config.mts` 中的 `base` 配置：

```typescript
// 对于 username.github.io/JiETNG/
base: '/JiETNG/',

// 对于自定义域名
base: '/',
```

### 问题4: 样式加载失败

通常是base路径配置错误，确保：
- GitHub Pages URL: `base: '/JiETNG/'`
- 自定义域名: `base: '/'`

---

## 📦 文件结构

```
/Users/matsuki/Desktop/JiETNG/
├── .github/
│   └── workflows/
│       └── deploy-docs.yml       # GitHub Actions自动部署
├── docs/
│   ├── .vitepress/
│   │   ├── config.mts           # 网站配置
│   │   ├── theme/
│   │   │   ├── index.ts
│   │   │   └── style.css        # 自定义样式
│   │   ├── cache/               # 缓存（可删除）
│   │   └── dist/                # 构建输出（可删除）
│   ├── guide/                   # 指南文档
│   │   ├── introduction.md
│   │   ├── getting-started.md
│   │   └── binding.md           # ✨新增
│   ├── features/                # 功能文档
│   │   ├── b50.md
│   │   ├── search.md            # ✨新增
│   │   ├── friends.md           # ✨新增
│   │   ├── plates.md            # ✨新增
│   │   └── level-records.md     # ✨新增
│   ├── commands/                # 命令文档
│   │   ├── basic.md
│   │   └── advanced.md          # ✨新增
│   ├── more/                    # 其他页面
│   │   ├── license.md
│   │   ├── support.md
│   │   └── faq.md               # ✨新增
│   ├── index.md                 # 首页
│   ├── package.json             # 依赖配置
│   ├── quick-start.sh           # 快速启动脚本
│   ├── DEPLOYMENT_GUIDE.md      # 详细部署指南
│   └── 部署说明.md              # 本文件
```

---

## 🎯 推荐工作流程

### 新手推荐：

1. **先本地预览**
   ```bash
   cd /Users/matsuki/Desktop/JiETNG/docs
   ./quick-start.sh dev
   ```

2. **确认无误后推送到GitHub**
   ```bash
   git add .
   git commit -m "文档更新"
   git push origin main
   ```

3. **GitHub自动部署到线上**
   - 访问 https://matsuk1.github.io/JiETNG/

### 进阶用户：

直接编辑、提交、推送，GitHub自动处理一切：

```bash
# 编辑文件
vim docs/features/new-feature.md

# 提交并推送
git add docs/
git commit -m "添加新功能文档"
git push origin main

# GitHub Actions自动构建和部署
```

---

## 📚 相关文档

- **详细部署指南**: `DEPLOYMENT_GUIDE.md` - 包含所有细节和故障排除
- **VitePress配置**: `docs/.vitepress/config.mts` - 网站配置文件
- **GitHub Actions**: `.github/workflows/deploy-docs.yml` - 自动部署配置
- **快速启动脚本**: `docs/quick-start.sh` - 一键开发/构建/预览

---

## 🎉 现在就开始！

**立即预览文档：**

```bash
cd /Users/matsuki/Desktop/JiETNG/docs
./quick-start.sh dev
```

**或立即部署到GitHub Pages：**

```bash
cd /Users/matsuki/Desktop/JiETNG
git add .
git commit -m "初始化文档网站"
git push origin main
```

然后访问：https://github.com/Matsuk1/JiETNG/settings/pages 启用GitHub Pages

---

**有问题？** 查看 `DEPLOYMENT_GUIDE.md` 获取详细帮助！
